*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
*.apk
*.aab
keystore.properties
*.keystore
*.jks
# Exception: the debug keystore is committed on purpose so CI-built debug APKs
# keep a stable signature and can be installed as updates. It holds the standard
# public Android debug credentials and cannot sign a release.
!app/debug.keystore

# Android SDK (should not be in repo)
/build-tools/
/platform-tools/
/platforms/
/licenses/
/emulator/
/sources/
/system-images/
/tools/
/cmdline-tools/
